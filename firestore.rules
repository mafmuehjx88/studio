
rules_version = '2';
service cloud.firestore {
  
  match /databases/{database}/documents {
    
    function isAdmin() {
      return request.auth.token.email == 'marrci448@gmail.com' || request.auth.token.email == 'ohshif5@gmail.com';
    }
    
    match /users/{userId} {
      allow read, update: if request.auth.uid == userId || isAdmin();
      allow create: if request.auth.uid == userId;
      allow list: if isAdmin();
    }
    
    match /users/{userId}/orders/{orderId} {
      allow read, create, update: if request.auth.uid == userId || isAdmin();
    }
    
    match /topUpRequests/{requestId} {
      allow create: if request.auth.uid != null;
      allow get: if request.auth.uid == resource.data.userId || isAdmin();
      allow list: if isAdmin();
      allow update: if isAdmin();
    }

    match /settings/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /announcements/{announcementId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /smileCodes/{codeId} {
        allow read, list, create, update, delete: if isAdmin();
        // Allow a user to read a code only if it's assigned to them (for history)
        // This is a future-proofing rule, current logic uses order history.
        allow get: if request.auth.uid == resource.data.usedBy;
    }

    match /{path=**}/orders/{orderId} {
      allow get, list: if isAdmin();
    }
  }
}
