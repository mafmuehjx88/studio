rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isAdmin() {
      return request.auth.token.email == 'ohshif5@gmail.com';
    }
    
    match /users/{userId} {
      allow read, update: if request.auth.uid == userId || isAdmin();
      // Allow user creation if the user is creating their own document
      allow create: if request.auth.uid == userId;
    }
    
    match /users/{userId}/orders/{orderId} {
      allow read, create: if request.auth.uid == userId;
      // Admin access via collection group query
    }
    
    match /topUpRequests/{requestId} {
      allow create: if request.auth.uid != null;
      allow read: if request.auth.uid == resource.data.userId || isAdmin();
      allow update: if isAdmin();
    }

    match /settings/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /announcements/{announcementId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Allow admins to list documents from collection groups
    match /{path=**}/orders/{orderId} {
      allow get, list: if isAdmin();
    }
  }
}

    